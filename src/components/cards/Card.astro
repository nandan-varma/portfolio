---
interface Props {
	class?: string;
	reveal?: boolean;
	[key: string]: unknown;
}

const { class: className = "", reveal = false, ...rest } = Astro.props;
---

<div
	class:list={["card overflow-hidden relative", className]}
	data-reveal={reveal ? "" : undefined}
	{...rest}
>
	<slot />
</div>
